-- +goose Up
-- +goose StatementBegin
SELECT 'init migration — placeholder for first schema changes';
-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin
SELECT 'down migration — placeholder';
-- +goose StatementEnd
